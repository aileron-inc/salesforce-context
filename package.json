{
  "name": "salesforce-context",
  "version": "0.0.0",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.19.0",
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.115.0"
  },
  "packageManager": "bun@1.3.14",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "cf-typegen": "wrangler types",
    "check": "bun run cf-typegen && tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "type": "module"
}
